# Set New Version

Standalone Flutter helper for Ventio on Windows.

What it does:
- Reads the current version from the main Ventio `pubspec.yaml`
- Lets you enter a new version like `1.0.49+59`
- Updates the local version files
- Runs the Windows installer build script

Launch it with `Set-New-Version.cmd`.
