Ventio update implemented in this package:

- Fixed previous app.dart compile issue.
- Kept delayed sync startup after login and sync stop on logout.
- Keep me signed in is initialized from saved preference and login persistence stays opt-in.
- Sales mobile layout uses product-first flow, bottom checkout bar, cart/checkout bottom sheets, embedded scanner preview, continuous scan, duplicate-scan guard, click sound, and haptic feedback.
- Client maintenance buttons are separated from Host reset behavior.
- Client Clear Local Data is local-only and does not write a sync reset event.
- Client Rebuild From Host clears local business data and pulls a fresh Host LAN/Cloud snapshot/events.
- AppIdentity now includes a per-device token and transportType. Store ID is read-only for Clients in the system foundation editor.
- Cloud requests include device headers. Backend endpoints have optional device-token authorization controlled by REQUIRE_DEVICE_TOKEN_AUTH=true after devices are re-paired.
- Added docs/VENTIO_SYNC_V2_IMPLEMENTATION_NOTES.md.

Validation performed here: node --check passed for API JS files. Flutter SDK is not installed in this environment, so please run flutter analyze and flutter test locally.
